import { View } from 'react-native';

// This is a placeholder screen that should never be rendered
// The tab button redirects to the modal before this can be shown
export default function CreatePlaceholder() {
  return <View />;
}